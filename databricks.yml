bundle:
  name: qa-chatbot

workspace:
  profile: DEFAULT

variables:
  catalog:
    description: Unity Catalog catalog name
    default: main
  schema:
    description: Unity Catalog schema name
    default: qa_chatbot

include:
  - resources/*.yml
