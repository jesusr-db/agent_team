command:
  - uvicorn
  - src.app.backend.main:app
  - --host
  - 0.0.0.0
  - --port
  - "8000"

env:
  - name: RAG_SERVING_ENDPOINT
    value: qa-chatbot-rag
  - name: CATALOG
    value: ${var.catalog}
  - name: SCHEMA
    value: ${var.schema}
